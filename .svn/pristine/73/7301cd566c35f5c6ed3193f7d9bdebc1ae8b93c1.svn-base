import React from 'react';

import './index.css';

const Index = ({ label, value, ...rest }) => (
    <div className='field' {...rest}>
        <span className='label'>{label}</span>
        <span className='number'>{value}</span>
    </div>
);

export default Index;
